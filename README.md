# GitHub Material Icons

A cross-browser browser extension built with WXT that replaces GitHub's file and folder icons with Material Design icons.

## Development

```bash
npm install
npm run dev
```

Useful scripts:

- `npm run build`
- `npm run build:firefox`
- `npm run build:safari`
- `npm run compile`
- `npm run sync:assets`
